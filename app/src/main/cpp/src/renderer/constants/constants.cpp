#include "renderer/constants/constants.h"

namespace renderer {
    namespace constants {
        const unsigned int screen_width = 1200;
        const unsigned int screen_height = 720;
        const std::string window_title = "game";

        const engine::Color window_clear_color = engine::Color(0, 0, 0);
    } // constants
} // renderer